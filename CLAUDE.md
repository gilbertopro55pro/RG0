@AGENTS.md

# מילות קוד לפתיחת הודעה

כשהודעת המשתמש (בעברית, בכל שיחה) נפתחת באחת מהמילים הבאות, זה קובע את היקף העדכון שיש לבצע על השינוי המבוקש באותה הודעה, במערכת ה-**web** (RG0) בלבד — ראה למטה לגבי דסקטופ:

- **עדכון** — לממש את השינוי, להוסיף ערך חדש בראש מערך `CHANGELOG` ב-`src/lib/changelog.ts` (זה מה שמעלה את `CURRENT_VERSION`, הנגזר מ-`CHANGELOG[0]`), וגם לעדכן את דף הנחיתה (`src/app/landing/page.tsx`) אם השינוי רלוונטי לשיווק/למה שמוצג שם.
- **עדכון אדמין** — לממש את השינוי אבל לחסום אותו לחשבון האדמין בלבד, לפי `ADMIN_EMAIL` מ-`src/lib/admin.ts` — התבנית הקיימת בקוד היא "זמין כרגע רק בחשבון האדמין, עד להשלמת הליטוש" (ראה entry של גרסה 1.93.0 ב-CHANGELOG לדוגמה קונקרטית). בלי entry ב-CHANGELOG כל עוד זה חסום למשתמשים.
- **עדכון ללא** — לממש את השינוי ולהוסיף entry חדש ב-CHANGELOG (מעלה גרסה), בלי לגעת בדף הנחיתה.
- **עדכון שקוף** — לממש את השינוי ולפרוס אותו חי, בלי להוסיף entry ב-CHANGELOG ובלי להעלות את מספר הגרסה — שינוי שקט שלא מופיע ב"מה חדש" למשתמש.

אלו לא מילים סתמיות בתוך משפט — הן תמיד מסמנות את סוג הפעולה שנדרש, בכל הודעה שנפתחת בהן.

## דסקטופ (photographer-flow-desktop)

מילות הקוד למעלה (CHANGELOG/דף נחיתה/חסימת אדמין) לא חלות על הדסקטופ — אין לו CHANGELOG/דף נחיתה/ADMIN_EMAIL משלו. לגבי הדסקטופ: **תמיד לבצע push (ומיזוג, כשרלוונטי) באופן אוטומטי**, בלי לחכות שהמשתמש יבקש את זה במפורש — ברגע ששינוי הושלם, נבדק ומוכן. הדסקטופ דוחף ישירות ל-`main` (אין שם שער PR), אז "push" הוא בפועל גם ה"מיזוג".

## להשלים כל עדכון עד הסוף — לא לעצור באמצע

כשמבצעים משימה (עדכון עם אחת המילים למעלה, או כל בקשה אחרת), יש להשלים אותה **עד הסוף בפועל** — כולל הרצת migration בדאטהבייס אם יש כזה, וכולל מיזוג ל-`main` (RG0: פתיחת PR ומיזוג; דסקטופ: push ישיר, כמו למעלה) — ולא להשאיר את זה כ"שלבים שהמשתמש צריך לבצע" כברירת מחדל. המשתמש רוצה לראות את השינוי חי מייד אחרי שנכתב "סיימתי", בלי עוד תחנות בדרך.

אם ממש חסר משהו שרק המשתמש יכול לספק (הרשאה, credential, אישור על פעולה מסוכנת) — לבקש את זה **במפורש ובבירור** (מה בדיוק חסר, ולמה) ולא סתם להפסיק ולזרוק רשימת "מה להריץ" על המשתמש. המשתמש אישר מראש: אם צריך ממנו גישה או אישור, לבקש ולקבל.

## הוראות שתלויות רק במשתמש — תמיד בעברית, בשלבים קלים

כל פעם שיש שלב במשימה שרק המשתמש יכול לבצע (כי אין לי גישה/הרשאה/יכולת מהסביבה הזו) — כמו הגדרת משתנה סביבה, יצירת credential, פעולה בממשק חיצוני — יש לכתוב את זה **בעברית**, בשלבים ברורים, ממוספרים, וקלים לביצוע (לא הנחיה כללית מעורפלת כמו "תגדיר גישה לדאטהבייס"). זו הוראה קבועה, לא ספציפית למשימה אחת.

## גישה לדאטהבייס (Supabase) — חסום מרשת, אל תנסה psql/API

**נבדק בפועל (2026-09-22) ואושר חסום**: הסביבה הזו (Claude Code on the web sandbox) חסומה ברמת מדיניות ארגונית לכל תעבורת רשת חוץ מ-HTTPS לרשימת דומיינים מצומצמת (Anthropic, npm, pypi וכו׳). זה חוסם גם:
- חיבור `psql` ישיר (`db.*.supabase.co:5432`, IPv6) — timeout ברמת ה-DNS/routing.
- חיבור `psql` דרך ה-pooler (`*.pooler.supabase.com:5432`, IPv4) — timeout, אין תעבורת TCP יוצאת לפורט הזה בכלל.
- קריאת HTTPS ישירה ל-`api.supabase.com` (ה-Management API) — נכשל עם `403 connect_rejected` מהפרוקסי, מדיניות ארגונית.

**שום connection string או טוקן לא יעזור** — זו לא בעיית auth, זו חסימת רשת מבנית. אל תבזבז זמן שוב בניסיון להתחבר ישירות לדאטהבייס מכאן.

**הדרך שעובדת בפועל להרצת migration**: לבקש מהמשתמש לפתוח **SQL Editor** בדשבורד של Supabase (בדפדפן שלו, לא כאן), להדביק שם את תוכן קובץ ה-migration, וללחוץ Run. זה 4 שלבים פשוטים, בלי credentials בכלל:
1. בתפריט הצד של Supabase: **SQL Editor**.
2. **New query**.
3. הדבקת ה-SQL (לתת אותו כטקסט מוכן להעתקה, לא רק "תריץ את הקובץ").
4. **Run**.

לוודא מהמשתמש שזה רץ בהצלחה (לא רק "לחצתי Run") לפני שממשיכים למיזוג.

## אבטחה — לעולם לא למסור מידע רגיש

בשום מצב — לא אני, ולא שום סוכן אחר שעובד על הפרויקט הזה (RG0 או photographer-flow-desktop) — ימסור, יחשוף, ידפיס, ישמור בקובץ, או יעלה ל-git: מפתחות API, סיסמאות (כולל סיסמת דאטהבייס), API secrets/tokens, connection strings עם credentials, או כל מידע רגיש של משתמשים או של האדמין (פרטי קשר, נתוני תשלום, מידע אישי וכו׳).

זה כולל בפרט:
- לא להדפיס secret בפלט צ'אט/פקודה בלי צורך טכני ממשי באותו רגע.
- לא לכתוב secret לשום קובץ שנכנס ל-git (גם לא בהיסטוריה/comment/דוגמה).
- לא לשלוח secret לשירות חיצוני שהמשתמש לא ביקש עבורו במפורש.
- אם secret כן נדרש לפעולה טכנית חד-פעמית (למשל סיסמת DB להרצת migration) — להשתמש בו רגע אחד בלבד, לא לחזור עליו/להציג אותו שוב אחרי השימוש, ולא להשאיר ממנו עקבות בשום קובץ, לוג, או commit.

זו הוראה קבועה ומחמירה — לא ספציפית למשימה אחת, וחלה על כל שיחה עתידית.
