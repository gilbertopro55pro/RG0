@AGENTS.md

# מילות קוד לפתיחת הודעה

כשהודעת המשתמש (בעברית, בכל שיחה) נפתחת באחת מהמילים הבאות, זה קובע את היקף העדכון שיש לבצע על השינוי המבוקש באותה הודעה, במערכת ה-**web** (RG0) בלבד — ראה למטה לגבי דסקטופ:

- **עדכון** — לממש את השינוי, להוסיף ערך חדש בראש מערך `CHANGELOG` ב-`src/lib/changelog.ts` (זה מה שמעלה את `CURRENT_VERSION`, הנגזר מ-`CHANGELOG[0]`), וגם לעדכן את דף הנחיתה (`src/app/landing/page.tsx`) אם השינוי רלוונטי לשיווק/למה שמוצג שם.
- **עדכון אדמין** — לממש את השינוי אבל לחסום אותו לחשבון האדמין בלבד, לפי `ADMIN_EMAIL` מ-`src/lib/admin.ts` — התבנית הקיימת בקוד היא "זמין כרגע רק בחשבון האדמין, עד להשלמת הליטוש" (ראה entry של גרסה 1.93.0 ב-CHANGELOG לדוגמה קונקרטית). בלי entry ב-CHANGELOG כל עוד זה חסום למשתמשים.
- **עדכון ללא** — לממש את השינוי ולהוסיף entry חדש ב-CHANGELOG (מעלה גרסה), בלי לגעת בדף הנחיתה.
- **עדכון שקוף** — לממש את השינוי ולפרוס אותו חי, בלי להוסיף entry ב-CHANGELOG ובלי להעלות את מספר הגרסה — שינוי שקט שלא מופיע ב"מה חדש" למשתמש.

אלו לא מילים סתמיות בתוך משפט — הן תמיד מסמנות את סוג הפעולה שנדרש, בכל הודעה שנפתחת בהן.

## פריסה ל-production — מיזוג ל-main לא אומר שזה חי!

מיזוג PR ל-`main` **לא** מפרסם אוטומטית את האתר ל-myframeflow.com. הפריסה מתחלקת לשניים (עודכן 2026-09-23):

1. **שרת ה-PDF (Fly.io)** — נפרס **אוטומטית** ע"י GitHub Actions (`.github/workflows/deploy-pdf-worker.yml`) בכל מיזוג ל-`main`, ורק אם קוד צינור הרינדור השתנה. ההשוואה: תגית ה-image החי (`render-<hash>`) מול hash של כל גרף ה-imports של `worker/src/index.ts` (`scripts/computeRenderHash.js`). הבדיקה עצמה: `scripts/flyWorkerIsCurrent.sh`. הטוקן שמור כ-GitHub secret בשם `FLY_API_TOKEN`.
   - **אי אפשר לפרוס ל-Fly מסביבת הענן של Claude Code** (נבדק 2026-09-23): ה-proxy של הסביבה מיירט TLS ושובר גם את depot וגם את ה-builder של Fly. לא לנסות שוב — רק דרך ה-workflow. `fly status` ו-Machines API (קריאה) כן עובדים.
2. **האתר (Vercel)** — `scripts/deploy.sh`, **שאני מריץ בעצמי מסביבת הענן** (יש `VERCEL_TOKEN`/`VERCEL_ORG_ID`/`VERCEL_PROJECT_ID` בסביבה, ו-api.vercel.com פתוח). הסקריפט קודם בודק שהשרת ב-Fly כבר על קוד הרינדור הנוכחי, ונכשל אם לא — אז מחכים שריצת ה-workflow על `main` תסתיים ומריצים שוב.

סדר גמר משימה ב-RG0: מיזוג ל-`main` → (אם נגעו בקוד רינדור: לחכות ל-workflow "Deploy PDF worker") → `git checkout main && git pull` בסביבה → `bash scripts/deploy.sh` → לוודא ב-API של Vercel ש-myframeflow.com מצביע לפריסה החדשה. זה חלק מגמר המשימה, לא שלב אופציונלי.

`vercel --prod` מעלה את **הקבצים המקומיים** של מי שמריץ, לא את `main`. לכן להריץ `deploy.sh` רק מ-checkout נקי של `main` העדכני — הרצה מתיקייה ישנה (למשל `~/Desktop/photographer-flow` אצל המשתמש בלי `git pull`) **תחזיר את האתר אחורה**. עדיף שהפריסה תרוץ רק מסביבת הענן.

## דסקטופ (photographer-flow-desktop)

מילות הקוד למעלה (CHANGELOG/דף נחיתה/חסימת אדמין) לא חלות על הדסקטופ — אין לו CHANGELOG/דף נחיתה/ADMIN_EMAIL משלו. לגבי הדסקטופ: **תמיד לבצע push (ומיזוג, כשרלוונטי) באופן אוטומטי**, בלי לחכות שהמשתמש יבקש את זה במפורש — ברגע ששינוי הושלם, נבדק ומוכן. הדסקטופ דוחף ישירות ל-`main` (אין שם שער PR), אז "push" הוא בפועל גם ה"מיזוג".

## להשלים כל עדכון עד הסוף — לא לעצור באמצע

כשמבצעים משימה (עדכון עם אחת המילים למעלה, או כל בקשה אחרת), יש להשלים אותה **עד הסוף בפועל** — כולל הרצת migration בדאטהבייס אם יש כזה, וכולל מיזוג ל-`main` (RG0: פתיחת PR ומיזוג; דסקטופ: push ישיר, כמו למעלה) — ולא להשאיר את זה כ"שלבים שהמשתמש צריך לבצע" כברירת מחדל. המשתמש רוצה לראות את השינוי חי מייד אחרי שנכתב "סיימתי", בלי עוד תחנות בדרך.

אם ממש חסר משהו שרק המשתמש יכול לספק (הרשאה, credential, אישור על פעולה מסוכנת) — לבקש את זה **במפורש ובבירור** (מה בדיוק חסר, ולמה) ולא סתם להפסיק ולזרוק רשימת "מה להריץ" על המשתמש. המשתמש אישר מראש: אם צריך ממנו גישה או אישור, לבקש ולקבל.

## הוראות שתלויות רק במשתמש — תמיד בעברית, בשלבים קלים

כל פעם שיש שלב במשימה שרק המשתמש יכול לבצע (כי אין לי גישה/הרשאה/יכולת מהסביבה הזו) — כמו הגדרת משתנה סביבה, יצירת credential, פעולה בממשק חיצוני — יש לכתוב את זה **בעברית**, בשלבים ברורים, ממוספרים, וקלים לביצוע (לא הנחיה כללית מעורפלת כמו "תגדיר גישה לדאטהבייס"). זו הוראה קבועה, לא ספציפית למשימה אחת.

## גישה לדאטהבייס (Supabase) — יש connector מחובר, תשתמש בו

**מ-2026-09-22: מחובר connector רשמי של Supabase (MCP)** — כלים כמו `mcp__Supabase__execute_sql`, `mcp__Supabase__apply_migration`, `mcp__Supabase__list_tables`, `mcp__Supabase__list_migrations`, `mcp__Supabase__query_logs`, `mcp__Supabase__get_advisors` זמינים ישירות (עולים דרך `ToolSearch` אם לא כבר טעונים). ה-project_id הנכון: `ojfhglvkolyjdcoqakqc` (photographer-flow-frankfurt, eu-central-1) — יש גם פרויקט אחר (`qlobqnxzuqsvixikkwkq`) ברשימה, לא רלוונטי, לא לגעת בו.

**זו הדרך המועדפת עכשיו להריץ migrations ולתקן תקלות בדאטה** — לא צריך יותר לשלוח את המשתמש ל-SQL Editor באופן שגרתי. שימוש: `mcp__Supabase__apply_migration` ל-DDL (עמודות/טבלאות חדשות), `mcp__Supabase__execute_sql` לתיקוני דאטה/בדיקות. שים לב: `list_migrations` לא משקף את כל הקבצים ב-`supabase/migrations/*.sql` בריפו (כל ה-migrations עד עכשיו רצו ידנית ב-SQL Editor, לא נרשמו במעקב הפנימי של Supabase) — זה לא בעיה, רק תדע שהמספור בין השניים לא בהכרח מסונכרן.

**המשתמש הבהיר את הכוונה מאחורי "גישה לכל" (2026-09-22)**: לא חשיפת מידע רגיש כלפי חוץ, אלא שכשהוא כותב שצריך לתקן/לבצע משהו בדאטהבייס — לבצע ישירות דרך ה-connector, בלי לשלוח אותו להריץ SQL בעצמו כל פעם. פעולות הרסניות אמיתיות (DELETE/DROP, שינוי נתוני תשלום/חיוב) עדיין מקבלות אישור קצר לפני שהן רצות — לא כי אין אמון, אלא כי זו פרקטיקה נכונה לכסף אמיתי. כל השאר (תיקוני דאטה, migrations רגילים, אבחון) — פשוט לבצע.

**אם ה-connector לא זמין בשיחה מסוימת** (למשל לא מחובר/מופעל): הנפילה חזרה היא SQL Editor ידני (4 שלבים: SQL Editor → New query → הדבקה → Run, לוודא הצלחה בפועל לפני מיזוג) — לא לנסות `psql`/Management API ישירות, זה חסום ברמת רשת (נבדק ואושר).

**MEMORY.md**: המשתמש מכין קובץ MEMORY.md עם סיכום פעולות שכבר בוצעו, כדי לשמור על סנכרון בין שיחות — לחפש ולקרוא אותו אם הוא קיים בתחילת כל משימה.

## אבטחה — לעולם לא למסור מידע רגיש

בשום מצב — לא אני, ולא שום סוכן אחר שעובד על הפרויקט הזה (RG0 או photographer-flow-desktop) — ימסור, יחשוף, ידפיס, ישמור בקובץ, או יעלה ל-git: מפתחות API, סיסמאות (כולל סיסמת דאטהבייס), API secrets/tokens, connection strings עם credentials, או כל מידע רגיש של משתמשים או של האדמין (פרטי קשר, נתוני תשלום, מידע אישי וכו׳).

זה כולל בפרט:
- לא להדפיס secret בפלט צ'אט/פקודה בלי צורך טכני ממשי באותו רגע.
- לא לכתוב secret לשום קובץ שנכנס ל-git (גם לא בהיסטוריה/comment/דוגמה).
- לא לשלוח secret לשירות חיצוני שהמשתמש לא ביקש עבורו במפורש.
- אם secret כן נדרש לפעולה טכנית חד-פעמית (למשל סיסמת DB להרצת migration) — להשתמש בו רגע אחד בלבד, לא לחזור עליו/להציג אותו שוב אחרי השימוש, ולא להשאיר ממנו עקבות בשום קובץ, לוג, או commit.

זו הוראה קבועה ומחמירה — לא ספציפית למשימה אחת, וחלה על כל שיחה עתידית.
